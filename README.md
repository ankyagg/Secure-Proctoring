
  # Build it now

  This is a code bundle for Build it now. The original project is available at https://www.figma.com/design/b0kPZf7cT5Q03zbPACYPms/Build-it-now.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  